<%= form_for(@train) do |f| %>
  <%= render 'common/errors', resource: @train %>


    <div>
      <%= f.label :number %>
      <%= f.number_field :number %>
    </div>


  <div>
    <%= f.label :route, "Current station: " %>
    <%= f.collection_select :route_id, Route.all, :id, :name %>
  </div>

  <div class="actions">
    <%= f.submit "#{ @train.new_record? ? "Create" : "Edit"}" %>
  </div>
<% end %>