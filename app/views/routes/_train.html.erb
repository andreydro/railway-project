<% if @route.trains.empty? %>
  <p>There is no trains on the route</p>
<% else %>
  <p>Thrains on the route:</p>
    <ul>
      <% @route.trains.each do |train| %>
        <li><%= train.number %></li>
      <% end %>
    </ul>
<% end %>