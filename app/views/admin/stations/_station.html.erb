<tr>
  <td><%= station.title %></td>

  <%= render "form", station: @station %>

  <td><%= link_to t('common.show'), [:admin, station] %></td>
  <td><%= link_to t('common.edit'), edit_admin_station_path(station) %></td>
  <td><%= link_to t.('common.destroy'), [:admin, station], method: :delete, data: { confirm: "#{ t.(c'ommon.are_you_sure') }" } %></td>
</tr>