<%= form_for(@station) do |f| %>
    <%= render 'common/errors', resource: station %>

    <div class="field">
      <%= f.label :title %>
      <%= f.text_field :title %>
    </div>
    <div class="actions">
      <%= f.submit "#{ @station.new_record? ? "Create" : "Edit"}" %>
    </div>
<% end %>